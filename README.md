# Assignment 2 – Cookies

This project demonstrates working with cookies in JavaScript.

## Features
- Shows a cookie consent modal on first visit.
- Optional settings modal with toggle switches.
- Stores browser, OS, and screen dimensions as cookies.
- Cookies expire after ~20 seconds.
- Uses `navigator` and `screen` objects.
- Includes `setCookie()` and `getCookie()` functions.

## Files
- `index.html`
- `style.css`
- `app.js`
- `/assets`

## Author
Riley Strauman
